import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';

class BackButton extends statelessWidget {

  final String route;
  final int size;
  final Color iconColor;
  const BackButton({Key? key, required this.route,this.size=30, this.iconColor=Colors.white });
  @override
  Widget build(){}
}